{
  "extends": "@tsconfig/node16/tsconfig.json",
  "compilerOptions": {
    "module": "esnext",
    "moduleResolution": "bundler",

    // Include DOM libs as a hacky way to get fetch types in global scope
    "lib": ["DOM", "DOM.Iterable"],
  }
}
